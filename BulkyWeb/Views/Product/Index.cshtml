@model List<Product>


<div class="container">
    <div class="row pt-4">
        <div class="col-12">
            <h1 class="text-secondary">This is the Home Page of Product</h2>
        </div>
    </div>
    <div class="row pt-4">
        
        <div class="col-6">
            <h2 class="text-primary">Product List</h2>
        </div>
        <div class="col-6 text-end">
            @* asp-controller="" we can skip this controller if we are making a call from a veiw with same Controller*@
            <a asp-action="Create" class="btn btn-primary">  
                <i class="bg-info bi-plus-circle"></i>  Create New Product
            </a> 
        </div>
    </div>
    
    <div class="row">
        <table class="table table-bordered table-stripped">
            <thead>
                <tr>
                    <th>
                        Product ID
                    </th>
                    <th>
                        Product Title
                    </th>
                    <th>
                        Display ISBN
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var obj in Model.OrderBy(u => u.Id))
                {
                    <tr>
                        <td>
                            @obj.Id
                        </td>
                        <td>
                            @obj.Title
                        </td>
                        <td>
                            @obj.ISBN
                        </td>
                        <td>
                            <div class="w-75 btn-group" role="group">
                                <a asp-controller="Product" asp-action="Edit" asp-route-Id ="@obj.Id" class="btn btn-primary mx-2">
                                    <i class="bi bi-pencil-square"></i>Edit
                                </a>
                                <a asp-controller="Product" asp-action="Delete" asp-route-Id="@obj.Id" class="btn btn-danger mx-2">
                                    <i class="bi bi-trash-fill"></i>Delete
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
   
</div>
